services:
  - type: web
    name: cyberguard-dashboard
    env: docker
    plan: free
    disk:
      name: data
      mountPath: /var/data
      sizeGB: 1
    envVars:
      - key: DB_PATH
        value: /var/data/data_access_logs.db
      - key: PORT
        value: 8080
